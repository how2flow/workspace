#!/bin/bash

case ${DISTRO} in
  focal)
  ;;
  jammy)
  sed -i 's/foxy/humble/g' ${INSTALL_DIR}/configure/*
  sed -i 's/foxy/humble/g' ${INSTALL_DIR}/pkglist/*
  sed -i 's/foxy/humble/g' ${INSTALL_DIR}/scripts/*
  ;;
esac
